/**
 * {{pkg.description}}
 * @module {{pkg.name}}
 * @version {{pkg.version}}
 */

'use strict'

const create = require('./create')
const SGQueue = require('./sg_queue')

let lib = create.bind(this)

Object.assign(lib, {
  create,
  SGQueue
})

module.exports = lib